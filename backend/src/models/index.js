const userModel = require('./userModel');
const commentModel = require('./commentModel');
const userMovieModel = require('./userMovieModel');

module.exports = {
  userMovieModel,
  userModel,
  commentModel,
};
